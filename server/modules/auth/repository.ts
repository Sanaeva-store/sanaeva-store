// src/modules/auth/repository.ts
// import { db } from '../../db/client'   // your DB client (e.g. Prisma, Kysely)

export const findUserByEmail = async (email: string) => {
//   return await db.selectFrom('users')
//                .selectAll()
//                .where('email', '=', email)
    //                .executeTakeFirst()
    return null;
}