// Purchasing suppliers — reuses the inventory suppliers page component (same API + i18n namespace).
export { default } from "@/app/[locale]/(admin-dasboard)/admin-dasboard/inventory/suppliers/page";
